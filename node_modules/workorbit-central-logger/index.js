const { LoggerService } = require("./src/services/LoggerService");

// ✅ Export the logger properly
module.exports = {
    LoggerService,
    logger: new LoggerService(), // Default instance
    generateRequestId: require("uuid").v4,
};
